import SectionTitle from "./SectionTitle";

export default function Contact() {
    return (
        <main id="contact">
            <SectionTitle>
                Let's get in <span>touch</span>
            </SectionTitle>
        </main>
    );
}
